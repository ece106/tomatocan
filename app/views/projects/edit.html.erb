<h4>Edit <%= @project.name %></h4>

<% if user_signed_in? %>
  <% if @project.user_id == current_user.id %>
      <div class="blacktxt" style= "text-align:left">
        <br><b>Add Patron Perk to be sold in support of Project <%= @project.name %>: </b><br>
        <% if @dropdown.any? %>
          <% @dropdown.each do |perk| %>
            <%= form_for(perk) do |f| %>
              <%= f.hidden_field :project_id, :value => @project.id %> 
              <%= f.submit("Add " + perk.name ) %><br> 
              <div class="covername"> <br> </div>
            <% end %>
          <% end %>
        <% elsif @perklist.any? %>
          Please add merchandise to your account that you are able to sell and deliver in support of <%= @project.name %> 
          <%= button_to(name = "Create Perk", new_merchandise_path, :method => :get) %>
        <% end %>  
      </div>

      <div class="blacktxt" style= "text-align:left">
        <br><b>Create a new Patron Perk </b><br>
        <%= button_to(name = "Enter Perk Info", new_merchandise_path, :method => :get) %>
      </div><br>
  
    <%= form_for(@project) do |f| %>
      <% if @project.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>
          <ul>
          <% @project.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="blacktxt">
        <b>Project Name </b><br>
        <%= f.text_field :name, :size => 80, placeholder: "The Best Project Ever" %>
        <br><br>
        <b>URL ending</b> - numbers and letters ONLY. <br />
        www.CrowdPublish.TV/projects/<%= f.text_field :permalink, :size => 50, placeholder:  "YourprojectURLEnding" %>  <br><br>

        Upload a picture that represents your project (.jpg, .gif, .jpeg, or .png) - <i>a wide, short picture works best</i>
        <br><%= f.file_field :projectpic %> <br>

        <b>About project </b> - You may add to this later<br>
        <%= f.text_area :mission, :cols => 82, :rows => 7, placeholder:  "What is your mission statement? What will you accomplish? If this project is a book chapter or a movie scene, how does this phase relate to the final product?" %><br><br>
        <b>Deadline </b><br>
        When does this project expire?<br>
        <%= f.date_select :deadline, selected: Date.today + 2.months, :start_year => Date.today.year, :end_year => Date.today.year + 1 %>
      </div>

      <div class="field" style= "text-align:left">
        <br><%= f.submit %><br>
      </div>
   
    <% end %>
  <% end %>
<% end %><br><br><br>
