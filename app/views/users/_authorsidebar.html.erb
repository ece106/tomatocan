<title> CrowdPublish.TV - <%= @user.name %> site</title>

<div class="fanbook">
<div class="col-md-3" >

<% if @user.books.any? %>
  <table CELLPADDING="0" CELLSPACING="8" WIDTH="400">
  <% @user.books.each do |book| %>
    <%= form_for(book) do |f| %>
      <tr><td width="50"; style="width:50px;height:70px;background-color:#dddddd;  text-align: center; line-height:70%" >
        <% if book.coverpic.present? && book.coverpic != '' %>
          <%= link_to image_tag(book.coverpic, :size => "50x65", :title => "Purchase " + book.title), book_path(:id => book.id)  %>
        <% elsif book.coverpicurl.present? && book.coverpicurl.match(/http/) %>
          <%= link_to image_tag(book.coverpicurl, :size => "50x65", :title => "Purchase " + book.title), book_path(:id => book.id) %>
        <% elsif book.title.match /[0-9A-Za-z]/ %> 
          <div class="covertitle">
            <%= link_to book.title, book_path(:id => book.id), :style => 'color:#880000', :title => "Purchase " + book.title %><p></div>
          <div class="covername" style= "color:#2c3651">
            <%= link_to @user.name, book_path(:id => book.id), :style => 'color:#2c3651',  :title => "Purchase " + book.title %>
          </div>
        <% end %>
      </td>
      <td style="line-height:94%">
        <div style="float:left;width:75%; font-weight:bold">
          <%= link_to book.title, book_path(:id => book.id), :style => 'color:#2c3651', :title => "Purchase " + book.title %>
        </div>
        <div style="float:right;width:25%;">
          &nbsp<%= book.genre %></div>
        <div class="fifty"><%= book.fiftychar %></div>
      </td></tr> 
    <% end %>
  <% end %>
  </table>
<% elsif %>
  <table CELLPADDING="0" CELLSPACING="8" WIDTH="400">
    <tr><td width="50"; style="width:50px;height:70px;background-color:#dddddd;  text-align: center; line-height:70%" >
      <%= link_to image_tag("bjBkCover170.png", :size => "50x65"), "http://BudgetJustified.com", :title => "Budget Justified", :target => "_blank" %>
      <td style="line-height:94%">
        <div class="sidebartitle" style="float:left;width:70%;">
          <%= link_to "Budget Justified: The Waste, Fraud & Abuse of Government Workers... by Lisa Schaefer", "http://BudgetJustified.com/book.html", :title => "Purchase Budget Justified: The Waste, Fraud & Abuse of Government Workers", :target => "_blank" %>
        </div>
        <div style="float:right;width:30%;">
          Nonfiction Comedy/Drama
        </div>
        <div class="fifty">
          <br><br><br>A few years ago, an FAA employee did something shockingly obscene in front of a female engineering contractor. The next day the woman was fired. So rather than go back to another FAA contractor position, she used her talents to produce Budget Justified, a movie, web series, and video-short reenacting what she witnessed in FAA and contractor offices.
        </div>
    </td></tr> 
  </table>
  <% if @user == current_user %>
    <p>The above book description will be replaced with a listing of your books after you enter information about your books. <p>
  <% end %>

<% end %>

</div>
</div>
