<% @title="CrowdPublish.TV: The Future of Empowerment is Your Story - edit user profile "  %>
<%= render 'authorform' %>

<% if @user == current_user %>

  <%= form_for @user, :html => {:multipart => true} do |f| %>
    <%= render 'shared/error_messages', :object => f.object %>

    <div id="bkinp" style="position:absolute; overflow:hidden; left:160px; top:80px; width:190px; z-index:3">
    Three genres that categorize your work <br>
    <%= f.text_area :genre1, :cols => 20, :rows => 1, placeholder: "Main genre of your writing" %><br>
    <%= f.text_area :genre2, :cols => 20, :rows => 1, placeholder: "Sub genre" %><br>
    <%= f.text_area :genre3, :cols => 20, :rows => 1, placeholder: "Another genre" %>
    </div>

    <div id="bkinp" style="position:absolute; overflow:hidden; left:0px; top:250px; z-index:3; width:350px;">
    <p>Upload a picture of yourself (square is best)
    <br><%= f.file_field :profilepic %> OR

    <p> URL to a picture of you <br>
      <%= f.text_area :profilepicurl, :cols => 40, :rows => 1, placeholder:  "URL for profile pic (Square is best)" %>
    <br>URL to your blog <br>
      <%= f.text_area :blogurl, :cols => 40, :rows => 1, placeholder: "Blog URL" %>
    <br>Twitter handle (don't include the '@' symbol) 
      <%= f.text_area :twitter, :cols => 40, :rows => 1, placeholder: "Twitter" %>
<p>Change account type:
<p><%= f.radio_button(:author, 1) %>
<%= label_tag(:author_child, "Reader") %>
<br><%= f.radio_button(:author, 2) %>
<%= label_tag(:author_adult, "Author") %>
<br><%= f.radio_button(:author, 3) %>
<%= label_tag(:author_adult, "Organization") %>
    </div>
  

    <div id="passwd" style="position:absolute; overflow:hidden; left:380px; top:40px; z-index:3"><span class="Body-C0">
    <table>
      <tr>
        <td> Password:</td>
        <td><%= f.password_field :password %></td>
      </tr> <tr>
        <td>Password Confirmation:</td>
        <td><%= f.password_field :password_confirmation %></td>
      </tr> 
    </table>

    <center><%= f.submit "Save user Info" %> </center>
You must enter & confirm password every time you visit this page.
    After saving, you will be directed to your user profile. You may come back to this page to edit your info.
    </div>

    <div id="signup" style="position:absolute; overflow:hidden; 
left:380px; top:200px; z-index:3">

    In future versions of CrowdPublish.TV, we will automate linking your account with livestreaming video during signup. But for now, to change the default CrowdPublish.TV video on your Author Home Page to your own livestreaming video, sign up at <a href= "http://Ustream.TV" target="_blank"> Ustream </a>. 
    <p>Go to your Ustream dashboard (click on the icon in upper right of your <a href= "http://Ustream.TV" target="_blank"> Ustream.TV </a> page). Click on <b>Channel Settings</b> then click on <font color = "0000FF"> Embed</font> on the left side of your screen. You will see the code to Embed the <font color = "0000FF"> Live </font> video player. It looks something like the default code below. 
<br> 
Copy & paste the Live code from your Ustream account to replace the default code below. <br />
    <%= f.text_area :ustreamvid, :cols => 70, :rows => 5, placeholder:  '<iframe src="http://www.ustream.tv/embed/13434383" width="608" height="368" scrolling="no" frameborder="0" style="border: 0px none transparent;"></iframe><br /><a href= "http://www.ustream.tv/producer?utm_campaign=Embed+Producer+Promotion&utm_source=Web&utm_medium=Embed&utm_term=Producer+Page&utm_utm_content=Free+desktop+streaming+application+by+Ustream" style="padding: 2px 0px 4px; width: 400px; background: #ffffff; display: block; color: #000000; font-weight: normal; font-size: 10px; text-decoration: underline; text-align: center;" target="_blank">Free desktop streaming application by Ustream</a>' %>
    <p>On the same Embed page in your Ustream account, click on the <font color = "0000FF">Social Stream</font> tab. You will see code similar to the default code below. 
    Copy & paste the Social Stream code from your Ustream account to replace the default code below. <br />
    <%= f.text_area :ustreamsocial, :cols => 70, :rows => 3, placeholder: '<iframe width="468" scrolling="no" height="586" frameborder="0" style="border: 0px none transparent;" src="http://www.ustream.tv/socialstream/13434383"></iframe>' %>

    <p>Tell us all about yourself. <br />
    <%= f.text_area :about, :cols => 70, :rows => 7, placeholder: 'About Me' %>
    </div>

  <% end %>
<% end %>

<div id="userlist" style="position:absolute; overflow:hidden; left:0px; top:330px; z-index:3">

<%= form_for(@user, :html => { :multipart => true }) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
    </div>
  <% end %>
  </div>
<% end %>

</body>