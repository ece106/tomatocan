<% @title="CrowdPublish.TV - The Future of Empowerment is Your Story "  %>

<%= render 'authorform' %>
<%= render 'authorsidebar' %>

<% if @user == current_user %>

<div id="bkinp" style="position:absolute; overflow:hidden; left:380px; top:100px; z-index:3">

<%= form_for@book, :html => {:multipart => true} do |f| %>
  <%= render 'shared/error_messages' %>
  <center><span class="Body-C5">Enter Book info</span></center>
  <%= f.hidden_field :user_id %>

  <table width="400">
  <tr>
    <td>
<div style="float:left;width:75%;">
<%= f.text_area :title, :cols => 50, :rows => 2, placeholder: "Book title" %></div>

  <div style="float:right;width:25%;">
<%= f.text_area :genre, :cols => 15, :rows => 1, placeholder: "Genre" %></div>
</td></tr>

    <td><%= f.text_area :blurb, :cols => 70, :rows => 6, placeholder: "Book jacket blurb" %>
 </td></tr>
    <td><%= f.text_area :fiftychar, :cols => 70, :rows => 1, placeholder: "Brief sentence about book - MUST be less than a hundred characters" %>
 </td>  </tr>
  </tr>
  <tr>
    <td >Date book is estimated to be or was released
    <%= f.date_select :releasedate %>
 </td>
  </tr>
    <tr><td><%= f.text_area :coverpicurl, :cols => 70, :rows => 1, placeholder: "URL to a PICTURE of bookcover (must end in .jpg, .gif, .tga, or .png)" %><br> The easiest way to obtain the URL is to go to a site that has a picture of your book cover, right click on the picture, select Copy image URL, and paste here. *This MUST include the 'http://' part of the URL in order to work. Do not post the URL to a webSITE. Only PICTURES will work. (In the near future you'll be able to upload pictures.) <p> 

<%
=begin %>
    Upload a picture of your book cover 
    <%= f.file_field :coverpic %>
</tr>

    Upload your ebook, as a pdf or other electronic file 
    <%= f.file_field :bookpdf %>
</tr>
<%
=end %>

  </table>
  <%= f.submit "Enter New Book" %>
<% end %>
<br><br><br>

<% if @user.books.any? %>

<% @booklist.each do |bk| %>
   <%= form_for(bk) do |f| %>

  <table width="400">
  <tr>
    <td>
<div style="float:left;width:75%;">
<%= f.text_area :title, :cols => 50, :rows => 2, placeholder: "Book title" %></div>

  <div style="float:right;width:25%;">
<%= f.text_area :genre, :cols => 15, :rows => 1, placeholder: "Genre" %></div>
 </td></tr>
    <td><%= f.text_area :blurb, :cols => 70, :rows => 6, placeholder: "Book jacket blurb" %>
 </td>  </tr>
    <td><%= f.text_area :fiftychar, :cols => 70, :rows => 1, placeholder: "Brief sentence about book - MUST be less than a hundred characters" %>
</td></tr>
  <tr>
    <td >Date book is estimated to be or was released
    <%= f.date_select :releasedate %>

<p>

    Upload a picture of your book cover 
    <%= f.file_field :coverpic %>

 </td>
  </tr>
    <tr><td><%= f.text_area :coverpicurl, :cols => 70, :rows => 1, placeholder: "URL to a PICTURE of bookcover (must end in .jpg, .gif, .tga, or .png)" %> <br> *This MUST include the 'http://' part of the URL in order to work. Do not post the URL to a webSITE. Only PICTURES will work. (In the near future you'll be able to upload pictures.) <br> 
</tr>
    <tr><td>

<%
=begin %>
<p>    Upload your ebook, as a pdf or other electronic file 
    <%= f.file_field :bookpdf %>
<%
=end %>

 </td>
</tr>
  </table>

  <%= f.submit("Save Changes for " + bk.title ) %>
<br><br><br>
        <% end %>
      <% end %>
   <% end %>
<% end %>

</div>


<p id="notice"><%= notice %></p>

</body>