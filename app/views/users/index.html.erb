<% @title="CrowdPublish.TV Community " %>

<%= render 'form' %>

<%
=begin %>
<div id="userlist" style="position:absolute; overflow:hidden; left:455px; top:340px; z-index:3">
<%= button_to "Sign up ", new_user_path, :method=> :get %></div>
<%= link_to "Sign up", new_user_registration_path %> or
<%
=end %>

<div id="usernav" style="position:absolute; overflow:hidden; left:100px; top:300px; z-index:3">
  <% if user_signed_in? %>
    Signed in as <%= current_user.email %>. Not you?
    <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
  <% else %>
    Already a member? <%= link_to "SIGN IN", new_user_session_path %> <p>
  <% end %>
</div></div>

<div id="userlist" style="position:absolute; overflow:hidden; left:390px; top:320px; z-index:3">

<table CELLPADDING="0" CELLSPACING="10" WIDTH="600" >
  <tr>
    <th></th>
    <th><span class="Body-C3">  Featured People</span></th>
  </tr>
  <tr>
    <th></th>
    <th align="left">&nbsp&nbsp&nbsp&nbspName</th>
    <th align="left">&nbsp&nbsp&nbsp&nbspWriting Genres</th>
  </tr>
<% @users.each do |user| %>
  <tr>
    <td> 
  <% if user.profilepic.present? && user.profilepic != '' %>
    <%= image_tag(@user.profilepic, :size => "150x150") %>
  <% elsif user.profilepicurl %>
    <% if user.profilepicurl.match /http/ %>
      <%= link_to(image_tag(user.profilepicurl, :size => "50x50", :profilepicurl => user.name), user)  %>
    <% else %>
    <%= link_to(image_tag("http://gurutapas.com/images/whiteBk.jpg", :size => "50x50"), user)  %>   
    <% end %>
  <% else %>
      <%= link_to(image_tag("http://gurutapas.com/images/whiteBk.jpg", :size => "50x50", :profilepicurl => user.name), user) %> 
  <% end %>

</td>
    <td color="#443c55" align="left">
      <%= link_to user.name, user %></td>
    <td align="left"> <%= user.genre1 %>, <%= user.genre2 %>, <%= user.genre3 %></td>
  </tr>

<% end %>
</table>
</div>

