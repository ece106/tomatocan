<%= form_with(model: timeslot, local: true) do |form| %>
  <% if timeslot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timeslot.errors.count, "error") %> prohibited this timeslot from being saved:</h2>

      <ul>
      <% timeslot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= current_user.name %>
  </div>

  <div class="field">
    <%= form.label :start_at %>
    <%= form.datetime_select :start_at %>
  </div>

  <div class="field">
    <%= form.label :end_at %>
    <%= form.datetime_select :end_at %>
  </div>

  <div class="field"> 
    <%= form.radio_button :role, Option 1 %>
    <%= form.label 'Interviewer' %>
    <%= form.radio_button :role, :guest1_user_id %>
    <%= form.label 'Guest' %>
    <% if Option 1 == checked
      
    end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
