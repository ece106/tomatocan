<div class="row" >
  <div class="col-md-7 col-sm-offset-2 col-sm-8 col-sm-offset-1 col-xs-12 " style= "text-align:left" >
    <% if @group.user_id == current_user.id %>
      <% "link_to 'Delete Group', @group, method: :delete, data: { confirm: 'Are you sure?' } Don't really want this capability right now" %>
      <h1>Edit organization info</h1>

<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <b>Organization Name </b><br>
    <%= f.text_field :name, :size => 80 %>
  </div><br>
  <div class="field">
    <b>URL ending</b> for your organiztions's page - numbers and letters ONLY. <br />
    http://www.CrowdPublish.TV/<%= f.text_field :permalink, :size => 30, placeholder:  "YourGroupURL"  %>  
  </div><br>

  <div class="field">
    <b>Address</b> or zip code of typical meeting place <br>
    <%= f.text_field :address, :size => 80 %>
  </div><br>
  <div class="field">
    <b>About organization</b>  <br>
    <%= f.text_area :about, :cols => 82, :rows => 5 %>
  </div><br>
  <div class="field">
    <b>Get More Involvement</b> This is a call to action that will appear on your main page when your organization is supporting an active project. What would you like to ask them to do? Come to your gatherings? Support your sponsored projects? Be specific. <br>
    <%= f.text_area :callaction, :cols => 82, :rows => 5, placeholder: "Please support our projects by clicking on the button above." %>
  </div><br>
  <div class="field" style= "text-align:left">
      Organization type:
      <b>Organization's primary purpose for engaging with CrowdPublish.TV members:</b>
      <p><%= f.radio_button(:grouptype, "Project Supporter") %>
      <%= label_tag(:grouptype_child, "Project Supporter") %>
      <br><%= f.radio_button(:grouptype, "Book Club") %>
      <%= label_tag(:grouptype_adult, "Book Club") %>
      <br><%= f.radio_button(:grouptype, "Writers Organization") %>
      <%= label_tag(:grouptype_adult, "Writers Organization") %>
      <br><%= f.radio_button(:grouptype, "Informal Neighborhood Writer & Reader Gathering") %>
      <%= label_tag(:grouptype_adult, "Informal Neighborhood Writer & Reader Gathering") %>
      <br><%= f.radio_button(:grouptype, "Critique Group") %>
      <%= label_tag(:grouptype_adult, "Critique Group") %>
      <br><%= f.radio_button(:grouptype, "Other") %>
      <%= label_tag(:grouptype_adult, "Other") %>
  </div>
  <br><b>Twitter</b> handle (don't include the '@' symbol nor the entire url) <br> 
  <div class="blacktxt" style="vertical-align: text-top">
        <b>@</b> <%= f.text_area :twitter, :cols => 37, :rows => 1, placeholder: "twitterhandle" %>
  </div><br>
  <div class="field">
    Upload a banner picture that represents your group (.jpg, .gif, .jpeg, or .png) - <i>750x150px works best</i>
    <br><%= f.file_field :grouppic %> 
  </div><br>
  <div class="actions">
    <%= f.submit %>
  </div><br>
<% end %>

<%= link_to 'View Organization home page', @group %>
<% end %>

</div>
</div><br><br>