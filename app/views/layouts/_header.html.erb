<header>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#globalNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand">
                  <%= link_to image_tag("crowdPublishIcon.png", :style => "height:50px",:title => "CrowdPublish.TV Home"), "/" %>
                </a>
            </div>
            <div class="collapse navbar-collapse" id="globalNavbar">
                <ul class="nav navbar-nav">
                    <li class="navbar-text"><%= link_to "Discover Discussion Hosts", '/userswithmerch' %></li>
                    <% if user_signed_in? %>
                    <% else %>
                        <li class="navbar-text"><%= link_to "Create a page", '/signup' %></li>
                    <% end %>
                </ul>
                 
                <ul class="nav navbar-nav navbar-right">
                    <% if user_signed_in? %>
                        <li class="dropdown navbar-text">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"><%= current_user.name %></a>
                            <ul class="dropdown-menu">
                                <li>
                                    <%= link_to "Edit Profile", user_profileinfo_path(current_user.permalink), method: :get %>
                                </li>
                                <li>
                                    <%= link_to "Dashboard", user_dashboard_path(current_user.permalink), method: :get %>
                                    </li>
                                <li>
                                    <%= link_to "Create Perks", standardperks_path, method: :get %>
                                </li>
                            </ul>
                        </li>
                        <li>
                            <%= link_to "Sign out", destroy_user_session_path, :class => "btn btn-default navbar-btn", :method => :delete %>
                        </li>
                    <% else %>
                        <li>
                            <%= link_to 'Sign in', "/login", :class => "btn btn-default navbar-btn" %>
                        </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>
</header>