<header>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#globalNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <%= link_to image_tag("crowdPublishIcon.png", :class => "navbar-brand", :title => "CrowdPublish.TV Home", :height => "50px"), "/" %>
            </div>
            <div class="collapse navbar-collapse" id="globalNavbar">
                <ul class="nav navbar-nav">
                    <li><%= link_to "Discover Discussion Hosts", '/userswithmerch' %></li>
                    <% if user_signed_in? %>
                    <% else %>
                        <li><%= link_to "Create Page", '/signup' %></li>
                    <% end %>
                </ul>
                 
                <ul class="nav navbar-nav navbar-right">
                    <% if user_signed_in? %>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"><%= current_user.name %><span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li>
                                    <%= link_to "Edit Profile", user_profileinfo_path(current_user.permalink), method: :get %>
                                </li>
                                <li>
                                    <%= link_to "Dashboard", user_dashboard_path(current_user.permalink), method: :get %>
                                    </li>
                                <li>
                                    <%= link_to "Create Perks", standardperks_path, method: :get %>
                                </li>
                            </ul>
                        </li>
                        <li>
                            <button class="btn btn-default navbar-btn"><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></button>
                        </li>
                    <% else %>
                        <li>
                            <button class="btn btn-default navbar-btn"><%= link_to 'Sign in', "/login" %></button>
                        </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>
</header>