<header>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#globalNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand">
                  <%= link_to image_tag("crowdPublishIcon.png", :style => "height:50px;  margin-right:30px",:title => "CrowdPublish.TV Home"), "/" %>
                </a>
            </div>
            <div class="collapse navbar-collapse" id="globalNavbar">
                <ul class="nav navbar-nav">
                    <li><%= link_to "Discover Discussion Hosts", '/userswithmerch' %></li>
                    <li>
                        <% if user_signed_in? %>
                        <% else %>
                            <%= link_to "Create a page", '/signup' %>
                        <% end %>
                    </li>
                </ul>
                 
                <ul class="nav navbar-nav navbar-right">
                    <% if user_signed_in? %>
                      <li><p class="navbar-text"> <%= link_to current_user.name, user_profile_path(current_user.permalink), :class => "navbar-link" %></p></li>
                      <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete, :class => "btn btn-default navbar-btn" %></li>
                    <% else %>
                      <li><%= link_to 'Sign in', "/login", :class => "btn btn-primary navbar-btn" %></li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>
</header>