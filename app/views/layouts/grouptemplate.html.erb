<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=devicewidth, initial-scale=1">
  <title><%= full_title(yield(:title)) + " " + @group.name %></title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "https://js.stripe.com/v2/", "application", 'data-turbolinks-track' => true %>
  <link rel="shortcut icon" type="favicon.ico" href="http://www.ThinQ.tv/assets/crowdPublishIcon.png"/>
  <%= csrf_meta_tags %>
  <%#= tag :meta, :name => "stripe-key", :content => ENV['STRIPE_PUBLIC_KEY'] %>
  <% tag :meta, :name => "stripe-key", :content => 'STRIPE_PUBLIC_KEY' %>

  <% if content_for? :forusers %>
    <%= yield :forusers %>
  <% end %>

    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>
    <%= render 'layouts/groupform' %>
  <div id='sidebar'>
      <%= render 'layouts/groupsidebar' %>
  </div>

<body>
  <center>
    <div class="container-fluid" >
      <%= yield %>
    </div>
    <% #= render 'layouts/footer' %>
  </center>

</body>
</html>
