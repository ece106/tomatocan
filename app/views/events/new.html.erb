<div class="container">
  <div class="row" >
    <h1>Post a New Conversation Time</h1>
    <p>
      By creating an event below, the details of your conversation will be
      posted on the Home page of ThinQ.tv. This will allow your viewers to
      easily find the time and date of your conversation.
    </p>
  </div>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>
      <ul>
        <% @event.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <%= form_for @event do |f| %>
      <%= render "events/form", f: f, event: @event %>

      <%= f.submit "Post Conversation ", class: "btn btn-lg btn-primary create-event-btn", id: "eventSubmit" %>
    <% end %>
  </div>
</div>
