<div>
  <h4>Upcoming Conversations</h4>

  <h3>--Next conversation in: 04:21:21--</h3>

  <ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true"><h3>Tech Tues</h3></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false"><h3>Faith Wed</h3></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false"><h3>International Thurs</h3></a>
    </li>
  </ul>

  <div class="tab-content" id="myTabContent">
    <br />
    <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
      <% @events.each do |event| %>
        <div style="display: flex;">
          <div style="flex: 1;">
            <p class="lead"><%= link_to event.name, "#" %></p>
          </div>
          <div style="flex: 1;">
            <p class="lead" style="text-align: right;"><%= distance_of_time_in_words(Time.now, event.start_at) %></p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">
      <% @events.each do |event| %>
        <div style="display: flex;">
          <div style="flex: 1;">
            <p class="lead"><%= link_to event.name, "#" %></p>
          </div>
          <div style="flex: 1;">
            <p class="lead" style="text-align: right;"><%= distance_of_time_in_words(Time.now, event.start_at) %></p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">
      <% @events.each do |event| %>
        <div style="display: flex;">
          <div style="flex: 1;">
            <p class="lead"><%= link_to event.name, "#" %></p>
          </div>
          <div style="flex: 1;">
            <p class="lead" style="text-align: right;"><%= distance_of_time_in_words(Time.now, event.start_at) %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
