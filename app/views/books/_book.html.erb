<table CELLPADDING="0" CELLSPACING="8" WIDTH="620">
  <tr>
    <td > </td>
    <td>
    <div style="float:left;width:75%;">
      <b><%= link_to(book.title, book) %></b> </a></div>
    <div style="float:right;width:25%;">&nbsp&nbsp<%= book.genre %></div>
    </td></tr>
  <tr>
    <td style="width:75px;height:98px;background-color:#dddddd; " >

    <% if book.coverpic.present? && book.coverpic != '' %>
      <%= image_tag(book.coverpic, :size => "75x98", :title => "Purchase " + book.title) %>
    <% elsif book.coverpicurl.present? && book.coverpicurl.match(/http/) %>
      <%= image_tag(book.coverpicurl, :size => "75x98", :title => "Purchase " + book.title) %>
    <% elsif book.title.match /[A-Za-z]/ %> 
      <div class="covertitle">
      <%= link_to book.title, book_path(:id => book.id), :style => 'color:#880000', :title => "Purchase " + book.title %><p></div>
      <div class="covername" style= "color:#2c3651">
      <%= link_to @user.name, book_path(:id => book.id), :style => 'color:#2c3651',  :title => "Purchase " + book.title %>
      </div>
    <% end %>
    </td>
    <td class="blurb" style= "color:2c3651"> <%= book.blurb %></td>
</td>
  </tr>
</table>
